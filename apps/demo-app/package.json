{
  "name": "@obs/demo-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no linter configured'",
    "test": "echo 'no tests yet'",
    "load": "tsx src/load-generator.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@obs/sdk": "workspace:*",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
