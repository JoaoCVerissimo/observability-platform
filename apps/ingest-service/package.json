{
  "name": "@obs/ingest-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:consumer": "tsx watch src/consumer.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "start:consumer": "node dist/consumer.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no linter configured'",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@obs/shared": "workspace:*",
    "fastify": "^5.1.0",
    "@fastify/cors": "^10.0.0",
    "kafkajs": "^2.2.4",
    "@clickhouse/client": "^1.4.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
